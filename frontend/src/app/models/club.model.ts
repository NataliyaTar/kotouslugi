export interface IValueClub {
    id: number;
    text: string;
    description: string;
    img: string;
  }

  export interface IClub {
    id: number;
    name: string;
    description: string;
    img: string;
}